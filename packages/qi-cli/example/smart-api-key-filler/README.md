# 智能密钥填充器

这个插件属于工具类增强插件，用来为 Agent-Qi 扩展批量填充 API Key 的能力。

## 扩展了什么功能

- 注册 `smartApiKeyFiller` 内置工具，供系统或 Agent 调用
- 支持一次性为多个模型提供商批量写入 API Key
- 可自动把密钥更新到系统设置，省去逐个保存
- 会返回详细结果报告，便于确认成功和失败项

## 适合的场景

- 首次安装后批量配置多个平台密钥
- 一次性轮换多组 API Key
- 让 Agent 辅助完成提供商初始化配置
