# ModelScope 图像生成插件

集成魔搭 (ModelScope) 社区的 AIGC 图像生成能力。

## 主要功能

- **注册表集成**：将 `modelscope` 注册为全局模型工厂。
- **多样化模型**：支持调用 ModelScope 平台上托管的各类开源图像生成模型。
- **标准接口**：遵循应用的统一图像生成接口，与其他提供商无缝切换。

## 使用方法

1. 启用插件。
2. 在“模型提供商”设置中，添加类型为 `modelscope` 的提供商。
3. 配置相应的 API 访问权限，即可开始使用魔搭社区的生成能力。
